# ──────────────────────────────────────
# ATOM 2K26 — Environment Configuration Template
# Copy this to .env and fill in the values
# ──────────────────────────────────────

# Environment
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://localhost:27017/atom2k26

# Server
PORT=3000
HOST=0.0.0.0

# Session (generate a 64-char random hex)
SESSION_SECRET=your_64_char_hex_secret

# Admin (bcrypt hash of your admin password)
ADMIN_PASSWORD_HASH=your_bcrypt_hash

# SMTP Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your_email@psgtech.ac.in
SMTP_PASS=your_app_password
SMTP_FROM="ATOM 2K26 <your_email@psgtech.ac.in>"
